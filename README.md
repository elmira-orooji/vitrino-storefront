# Vitrino Storefront

طراحی و پیاده‌سازی یک فروشگاه اینترنتی فارسی با React. تمرکز پروژه روی تجربه خرید سریع، نمایش درست اطلاعات محصول و رابط واکنش‌گراست.

## مسیر فعلی پروژه

- تعریف MVP فروشگاه و نیازمندی‌های UX — انجام شد
- معماری اطلاعات و جریان خرید — انجام شد
- بازطراحی هویت بصری فروشگاه — انجام شد
- طراحی UI صفحه خانه و موبایل — انجام شد
- راه‌اندازی React و پیاده‌سازی App Shell — انجام شد
- پیاده‌سازی Hero، دسته‌بندی‌ها و پیشنهادهای ویژه صفحه خانه — انجام شد
- پیاده‌سازی فهرست محصولات، مرتب‌سازی و فیلترها — انجام شد
- پیاده‌سازی جزئیات محصول با الگوهای 21st و فونت محلی — انجام شد

نیازمندی‌های نسخه فروشگاهی در [سند محصول Storefront](docs/storefront/product-requirements-fa.md) قرار دارد.

ساختار صفحات و جریان خرید در [معماری اطلاعات Storefront](docs/storefront/information-architecture-fa.md) تعریف شده است.

مشخصات طراحی نهایی خانه در [UI صفحه خانه](docs/storefront/home-ui-fa.md) قرار دارد.

## محدوده MVP

- صفحه خانه
- دسته‌بندی و فهرست محصولات
- جست‌وجو و فیلتر
- جزئیات محصول
- سبد خرید
- ورود و ثبت‌نام ساده
- انتخاب آدرس و ثبت سفارش آزمایشی

## اجرای محلی

برای اجرای پروژه و تست‌ها از Node.js نسخه ۲۴ استفاده کنید.

```bash
npm install
npm run dev
```

بررسی کیفیت:

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

فهرست محصولات در مسیر `/#products` قرار دارد. جست‌وجو و دسته انتخاب‌شده در آدرس صفحه نگه داشته می‌شوند؛ فیلترهای دیگر تا زمانی که روی همان صفحه هستید فعال می‌مانند.

صفحه جزئیات با کلیک روی کارت محصول باز می‌شود؛ برای نمونه مسیر `/#product-nova-air-2`. گالری، رنگ، اندازه، تعداد، مشخصات و محصولات مرتبط در این صفحه قرار دارند. بازگشت به فهرست، فیلترها و جای قبلی صفحه را در همان نشست حفظ می‌کند.

در این مرحله ۱۲ محصول نمونه داریم. سبد خرید کامل هنوز پیاده‌سازی نشده است؛ دکمه افزودن فقط شمارنده سبد را به تعداد انتخاب‌شده تغییر می‌دهد و پس از refresh ذخیره نمی‌شود.

منابع کامپوننت و فونت در [یادداشت منابع رابط](docs/storefront/ui-sources-fa.md) ثبت شده‌اند.

## مسیر طراحی

طراحی از الگوهای رایج فروشگاه‌های بزرگ ایرانی الهام می‌گیرد: جست‌وجوی برجسته، دسته‌بندی قابل دسترس، کارت محصول متراکم و ناوبری ساده موبایل. هویت بصری، متن‌ها، تصاویر و کامپوننت‌ها مستقل طراحی می‌شوند.

## بخش مدیریت

کارهای قبلی پنل مدیریت حذف نشده‌اند و به‌عنوان فاز آینده پروژه نگهداری می‌شوند:

- [نیازمندی‌های پنل مدیریت](docs/product-requirements-fa.md)
- [معماری اطلاعات پنل](docs/information-architecture-fa.md)
- [وایرفریم‌های پنل مدیریت](docs/wireframes)

## Design System

- [Design System فروشگاه](docs/storefront/design-system-fa.md)
- [Storefront Tokens](design/storefront-tokens.json)
- [پایه‌های فعلی](docs/design-system-foundations-fa.md)
- [کامپوننت‌های پایه](docs/base-components-fa.md)
- [Design Tokens](design/tokens.json)
- [Component Tokens](design/components.json)

توکن‌های عمومی قبلی برای پنل مدیریت حفظ شده‌اند. پیاده‌سازی storefront از فایل `storefront-tokens.json` استفاده می‌کند.
